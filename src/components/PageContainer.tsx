/**
 * PageContainer component
 * Provides consistent page layout and spacing across the site
 */
import styled from 'styled-components'

export const PageContainer = styled.div`
  max-width: 1200px;
  margin: 0 auto;
  width: 100%;
` 