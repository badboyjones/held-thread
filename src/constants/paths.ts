/**
 * Centralizes all image and asset paths to maintain consistency
 * across the application
 */

export const PATHS = {
    images: {
        cards: 'src/images/card images/card images',
        knots: 'src/images/card images/knots',
        content: 'src/images/card images'
    }
} as const; 